package es.maestredam.geometria;

/**
 * 
 * Aplicación que bla bla
 * @version 1.1
 * @author illes
 * @date 16 feb 2022
 */
public class CiirculoApp {

    /**
     * Main method
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        
        Circulo grande = new Circulo (20,0); //Radio 20
        System.out.printf("Radio %2.f, color:%sH%n", copia.getRadio(), Integer.toHexString(copia.getColorRGB()));
    
    
        Circulo[] array=new Circulo[3];
        array[0]=normal;
        array[1]=copia;
        array[2]=new Circulo(100);
        //Recorro el array y cada objeto, escribo el radio
        for (Circulo c:array){
            System.out.println(c.getRadio());
            
        
        }
        int lista[]= new int [10];
    
    }

}
